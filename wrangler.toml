name = "cf-image-hosting"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 绑定 R2 存储桶
[[r2_buckets]]
binding = "MY_BUCKET"
bucket_name = "images" 

# 开启 Node.js 兼容性 (如果你后续需要用到 buffer 等 node 模块，不过 standard worker 尽量不用)
# node_compat = true
